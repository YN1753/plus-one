{
  "name": "plus-one",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "数字加1 — puzzle merge game core logic + GitHub Pages demo",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "typecheck": "node --check src/*.js"
  }
}
